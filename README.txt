Saumya Saxena
CS543-Assignment3

1) VMmanager.c – implements a virtual memory manager.

2) VMmanager_WithModification.c – implements a virtual memory manager with a physical address space smaller than the virtual address space.

3) VMDirtyBit.c – implements a virtual memory manager that handles the pages that have a dirty bit associated with it.

4) Additional.txt – contains the answers to the additional questions along with the extra credit question.

5) Statistics1.txt – contains the statistics for the Virtual Memory Manager.

6) Statistics2.txt – contains the statistics for the Virtual Memory Manager with smaller physical address space than virtual address space.

7) Statistics3.txt – contains the statistics for the Virtual Memory Manager that handles the dirty bit.
